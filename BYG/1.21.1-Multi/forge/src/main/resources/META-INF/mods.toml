modLoader = "javafml" #mandatory
loaderVersion = "${forge_loader_version_range}" #mandatory This is typically bumped every Minecraft version by Forge. See https://files.minecraftforge.net/ for a list of versions.

license = "${mod_license}"
issueTrackerURL="${issues_link}" #optional

[[mods]] #mandatory
modId = "${mod_id}" #mandatory
version = "${version}" #mandatory
displayName = "${mod_name2}" #mandatory
displayURL="${website_link}" #optional
logoFile="logo.png" #optional
authors = "${mod_authors}" #optional
description = "${mod_description2}" #mandatory

[[dependencies.${mod_id}]] #optional
    modId = "forge" #mandatory
    mandatory = true #mandatory
    versionRange = "[${forge_version},)" #mandatory
    ordering = "NONE" # The order that this dependency should load in relation to your mod, required to be either 'BEFORE' or 'AFTER' if the dependency is not mandatory
    side = "BOTH" # Side this dependency is applied on - 'BOTH', 'CLIENT' or 'SERVER'

[[dependencies.${mod_id}]]
    modId = "minecraft"
    mandatory = true
    versionRange = "${minecraft_version_range}"
    ordering = "NONE"
    side = "BOTH"

[[dependencies.${mod_id}]]
    modId="biomeswevegone"
    mandatory=true
    versionRange="NONE"
    ordering="BEFORE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="addonslib"
    mandatory=true
    versionRange="[1.21.1-1.13,)"
    ordering="BEFORE"
    side="BOTH"